direct_downstream_fixture:
  target: test
  outputs:
    test:
      type: sqlite
      threads: 1
      database: direct_downstream_fixture
      schema: main
      schemas_and_paths:
        main: direct_downstream_fixture.db
      schema_directory: .
